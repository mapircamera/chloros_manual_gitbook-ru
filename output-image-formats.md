---
metaLinks:
  alternates:
    - https://app.gitbook.com/s/o044KN3Ws0uIDvOmSkcR/output-image-formats
---

# Форматы выходных изображений

### TIFF (16-разрядный)

16-разрядный (цифровое значение пикселя от 0 до 65535) Вывод в формате TIFF (.tif).

### TIFF (32-разрядный, проценты %)

Вывод в 32-разрядном формате (плавающее значение пикселя 0,0–1,0) TIFF (.tif). Значения пикселей представляют собой проценты отражения, где 0,0 = 0 % и 1,0 = 100 %.

### **JPG (8-бит)**

8-бит (цифровое значение пикселя 0–255) Вывод в формате JPG (.jpg).

### **PNG (8-бит)**

8-битный (цифровое значение пикселя 0–255) формат вывода PNG (.png). Сжатие без потерь, подходящее для просмотра в Интернете и визуализации.

